Role: You are an expert Senior EDA Software Engineer specializing in Python, hardware description languages (Verilog/SystemVerilog), and the UVM Register Abstraction Layer (RAL).

Project Name: RegPulse (Step 2 - Advanced Side-Effects & Verification)
Goal: Extend the MVP to support complex register behaviors (W1C, RC, RS), hardware-to-register status reporting, and UVM backdoor path mapping.

Core Workflow Evolution:
1. Enhanced Input: The Excel spec now includes "Hardware Trigger" (Internal signal connection) and "Side Effect" (W1C, RC, etc.) columns.
2. Hardware Interface Generation: 
   - For every field, generate an output port `{field_name}_q` for hardware control.
   - For RO/Status fields, generate an input port `{field_name}_st` for hardware reporting.
3. Complex Logic Implementation:
   - W1C (Write 1 to Clear): If bus writes 1, clear the field; if hardware status is high, set/keep the field.
   - RC (Read to Clear): If bus reads the address, clear the field on the next cycle.
4. Verification Enhancement: Automatically generate UVM `hdl_path` for every register field to enable backdoor access.

Tasks for this Step:
1. Data Model Update: Update the 'Field' class to support 'hardware_interface' (input/output) and complex access policies (W1C, RS, RC).
2. Advanced RTL Generator (Jinja2):
   - Implement the logic for W1C (Write-1-to-Clear) using bitwise masking.
   - Automatically generate the module port list: include 'clk', 'rst_n', 'apb_interface', and all 'hw_side_interface' ports.
   - Ensure the 'case' statement handles read-clear (RC) logic correctly by updating the register value after a read transaction.
3. UVM RAL Backdoor Generator:
   - In the .sv file, use `add_hdl_path_slice` for each field to point to the correct hierarchical path in the RTL (e.g., "reg_inst.field_name_q").
   - Update the `uvm_reg_field::configure` call to reflect the correct 'volatile' and 'access' settings for RC/W1C fields.
4. Validation Engine Upgrade: 
   - Add a check to ensure 'RO' (Read-Only) fields have a corresponding hardware status input port defined.

Constraints:
- Environment: Use Python with the following path: D:/anaconda/envs/pytorch/python.exe.
- Logic Consistency: Ensure the 'reset_val' logic does not conflict with the 'W1C' or 'RC' behavior.
- Clean Code: Use Python f-strings or Jinja2 filters to handle the signal naming conventions (e.g., uppercase/lowercase conversion).

Deliverable: 
- Updated Python modules.
- A sample Verilog output showing the port list and W1C logic.
- A sample UVM RAL output showing the hdl_path configuration.