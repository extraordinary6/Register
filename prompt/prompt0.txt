Role: You are an expert Senior EDA Software Engineer specializing in Python, hardware description languages (Verilog/SystemVerilog), and the UVM Register Abstraction Layer (RAL).

Project Name: RegPulse
Goal: Develop a command-line and scriptable utility to automate the generation of synthesisable Verilog register banks and their corresponding UVM RAL models from an Excel-based specification.

Core Workflow of the Software:
1. Input: User provides an Excel (.xlsx) file containing register definitions (Name, Offset, Field, Bits, Access, Reset).
2. Analysis & Validation: 
   - Parse the Excel file into an Internal Representation (IR) using pandas.
   - Validation Rules: Check for address overlaps, bit-field collisions, and validate "Access Type" (RW, RO, W1C, RC, RS).
3. Generation Engine (MVP Rules):
   - Rule A (RTL Generation): Generate a Verilog module with a standard APB slave interface using a case statement for address decoding.
   - Rule B (UVM Generation): Generate a UVM RAL block file containing uvm_reg, uvm_reg_field, and uvm_reg_block classes.
4. Output: Clean, formatted .v and .sv files.

Tasks for this Step (MVP Foundation):
Please generate a structured Python project that implements the following:
1. Excel Parsing Backend: Use pandas to read the spec and convert it into a list of Register/Field objects.
2. Core Data Structures: Define classes for Register and Field with attributes like offset, width, lsb, access_type, and reset_val.
3. RTL Generator (Jinja2-based): Implement a template-based generator for 32-bit APB Verilog (supporting RW/RO first).
4. UVM RAL Generator: Implement a template-based generator for the .sv file including configure(), build(), and uvm_reg_map.
5. CLI Wrapper: A main.py that takes --input_excel and --output_dir as arguments.

Constraints:
- Environment: Use Python with the following path: D:/anaconda/envs/pytorch/python.exe.
- Modularity: Separate Parser, Internal Model, and Generators into distinct modules.
- Code Quality: Use Jinja2 templates for clean output.
- Error Handling: Raise ValidationError for address/bit overlaps.

Deliverable: Complete Python source code for the RegPulse MVP including a project directory structure and a sample Excel format description.